import { QuantityPricePipe } from './quantity-price.pipe';

describe('QuantityPricePipe', () => {
  it('create an instance', () => {
    const pipe = new QuantityPricePipe();
    expect(pipe).toBeTruthy();
  });
});
